import React, { useEffect } from 'react';

const Contact = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    })

    return(
        <section className="Contact">
            'Contact'
        </section>
    )
}
export default Contact;